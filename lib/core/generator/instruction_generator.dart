import 'package:way_to_class/core/models/route_segment.dart';

class InstructionGenerator {
  // templates mit keys wie corridor_straight, corridor_turn + parametern
  // keys haben mehrere werte wo zufällig entschieden wird
  List<String> generateInstructions(List<RouteSegment> route) {
    return [];
  }
}
